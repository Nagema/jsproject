const nameStr = "Ange";
const lastNameStr = "nagema";
const studentStr = nameStr.concat(" ", lastNameStr);
const studentTemplateString = `${nameStr} ${lastNameStr}`;
const estudianteMayus = studentStr.toUpperCase();
const estudianteMinus = studentStr.toLowerCase();
const studentStrLength = studentStr.length;
const beginsWith = studentStr.charAt();
const endsWith = studentStr.charAt(10);
const withoutSpaces = studentStr.replace(" ", "");
const includesName = studentStr.includes(nameStr);

